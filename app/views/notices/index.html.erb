


<head>
  
    <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

<nav class="navbar navbar-expand-md navbar-dark bg-dark ">
        <div class="mx-auto order-0">
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-collapse2">
            <span class="navbar-toggler-icon"></span>
          </button>
        </div>
        <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
            

              <% if current_user.role=='Hod' or current_user.role=='hod' %>
            
              <li> <a ><a class="btn btn-info ml-auto mr-1 btn-md bg-info" style="color: white"  href="notices/new">Create Notice</a></a></li>
            &nbsp
            <li> <a ><a class="btn btn-info ml-auto mr-1 btn-md bg-info" style="color: white"  href="group/group_home">Create Group</a></a></li>
            &nbsp
              <% end %>
             
            <li><a ><a class="btn btn-info ml-auto mr-1 btn-md bg-info" style="color: white" href="users/sign_out"> Logout</a></a></li>
            </li>
          </ul>
        </div>
      </nav>

</head>

<body>





<p id="notice"><%= notice %></p>


<div style="color:black;">

  <% if current_user.role =='admin' or current_user.role=='admin' %>
  <div>
  <%= form_tag notices_reg_id_path do %>
  <p>Enter ID</p>
  <%= text_field_tag(:regid) %>
  <br>
  <%= submit_tag "search" %>

  <% end %>

  <%= @id %>
 </div>





  
  <% end %>


<center><h1>Notices</h1>
<div class="table-responsive">
<table class="table" border="2px">
  <thead>
    <tr>
      <th>Notice ID</th>
      <th class="danger">Circular title</th>
      <th>Select file</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @notices.each do |notice| %>
      
      <tr>
        <% if current_user.role =='hod' or current_user.role =='Hod' %>
        <td><%= notice.id %></td>
        <td><%= notice.Circular_Title %></td>
        <% else %>
        <td> <%= notice.Circular_Title %></td>
        <% end %>
        <td><%= notice.Select_File %></td>
        <td><%= link_to 'Show', notice %></td>
        <% if  current_user.role =='hod' or current_user.role =='Hod' %>
        <td><%= link_to 'Edit', edit_notice_path(notice) %></td>
        <td><%= link_to 'Destroy', notice, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table></center> 
</div>
<p style="color: black;" >
</p>
<br>



<style >
  
.button {

  float: right;
  background-color: #a6b3f3; 
  border: none;
  color: white;
  padding: 20px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 4px 2px;
  cursor: pointer;
  border-radius: 12px;
  margin-right: 15px;
} 



</style>


<br><br>

<% if current_user.role =='hod' or current_user.role=='Hod' %>

 <%= form_tag notices_send_email_path do %>
 <p>Select</p>
 <%= select("branch", "id", @list) %>

 <br>
  <p>Email</p>
 <input type="text" name="mail"  pattern="[a-zA-Z0-9._-]+@[a-zA-Z]+.[a-zA-Z]{2,4}[.]{0,1}[a-zA-Z]{0,2}">

<p>Enter notice id</p>


 <input id="id"  type="text" name="id" required="required"> 
<br>

 <%= submit_tag "send email" %>
 <%end%>
  
<% end %>


 
</div>

</body>


